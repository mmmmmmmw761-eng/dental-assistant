function createSonlineAdapter() {
  throw new Error("Sonline adapter is not configured yet. Add API credentials and methods when a client needs Sonline.");
}

module.exports = { createSonlineAdapter };
